syntax = "proto3";

package proto.data;
option go_package="proto/data";

message ArticleData {
  int64  article_id = 1; /// 記事ID
  string title      = 2; /// タイトル
  string body       = 3; /// 内容
  int64  created_at = 4; /// 作成時刻(unixtime)
  int64  updated_at = 5; /// 更新時刻(unixtime)
}
